# Nakôbio × Biosain — Design System Unifié (Guide pour LLM / Agents IA)

> Système de conception unifié pour l'écosystème agroécologique & maraîcher :
> - Vitrine nako.bio (Mali / Sahel / Agroécologie & Pédagogie)
> - Biosain (Europe / Autriche / Maraîchage bio-intensif de précision & Semences)
> - Campus de formation (campus.nako.bio)
> - Documents techniques, storyboards et fiches pédagogiques imprimables A4.

---

## 1. Typographie Obligatoire & Tokens CSS
- Titres, Chiffres repères, Citations : `var(--font-serif)` (Playfair Display)
- Textes courants, UI, Badges, Tableaux, Boutons : `var(--font-sans)` (Quicksand)
- Interdiction formelle d'importer d'autres polices (Roboto, Montserrat, Poppins, etc.).

## 2. Thèmes & Identité de Marque
- Thème Nakôbio : `data-theme="nakobio"` (Vert Forêt Sahel #2F4A34, Ocre Latérite #C86832, Or Soleil #E0A030)
- Thème Biosain : `data-theme="biosain"` (Or Impérial Bio #CAAE00, Vert Forêt #2F4A34, Vert Siphon Profond #1E3324)

---

## 3. Catalogue des Composants CSS & Snippets HTML

### Snippet HTML: snippets/badges.html
```html
<!-- Snippet: Badge Certifié Bio -->
<span class="badge badge-bio">
  <span class="badge-dot"></span>
  100% Bio Certifié
</span>

<!-- Snippet: Badge Semence Paysanne Reproductible -->
<span class="badge badge-seed">
  <span class="badge-dot"></span>
  Semence Reproductible
</span>

<!-- Snippet: Badge Thème / Marque Principale -->
<span class="badge badge-brand">
  <span class="badge-dot"></span>
  Saison Sèche 2026
</span>

<!-- Snippet: Badge Accent Terroir -->
<span class="badge badge-accent">
  <span class="badge-dot"></span>
  Production Locale
</span>

<!-- Snippet: Badge Disponibilité Stock -->
<span class="badge badge-stock">
  <span class="badge-dot"></span>
  En stock (Planches A &amp; B)
</span>

<!-- Snippet: Badge Vigilance / Rupture imminente -->
<span class="badge badge-warning">
  <span class="badge-dot"></span>
  Dernières récoltes
</span>
```

### Snippet HTML: snippets/buttons.html
```html
<!-- Snippet: Bouton Primaire (Action principale / Commande / Inscription) -->
<button type="button" class="btn btn-primary">
  <span>Commander le panier</span>
</button>

<!-- Snippet: Bouton Secondaire (Détails / Fiche technique) -->
<button type="button" class="btn btn-secondary">
  <span>Voir la fiche technique</span>
</button>

<!-- Snippet: Bouton Accent / Latérite (Mise en avant terroir) -->
<button type="button" class="btn btn-accent">
  <span>Télécharger le guide PDF</span>
</button>

<!-- Snippet: Bouton Ghost (Action tertiaire / Lien discret) -->
<button type="button" class="btn btn-ghost">
  <span>Découvrir la méthode</span>
</button>
```

### Snippet HTML: snippets/callouts.html
```html
<!-- Snippet: Encadré Conseil / Bonne Pratique Agronomique -->
<div class="callout callout-tip">
  <div class="callout-title">
    <span>💡 Bonne pratique agronomique</span>
  </div>
  Toujours pailler immédiatement le sol après l'amendement (paille de mil, panicum ou résidus de fauche) afin de maintenir l'humidité et protéger la faune tellurique.
</div>

<!-- Snippet: Encadré Vigilance / Alerte Technique -->
<div class="callout callout-warning">
  <div class="callout-title">
    <span>⚠️ Point de vigilance</span>
  </div>
  Ne jamais tasser le lit de semence après le semis direct. Circuler exclusivement dans les passe-pieds de 40 cm prévus entre les planches.
</div>

<!-- Snippet: Encadré Danger / Interdiction Bio -->
<div class="callout callout-danger">
  <div class="callout-title">
    <span>⛔ Interdiction stricte en régie Bio</span>
  </div>
  Usage de tout intrant de synthèse (engrais chimiques NPK, pesticides organophosphorés) interdit. En cas de suspicion d'altises, recourir exclusivement au purin de neem ou de piment.
</div>
```

### Snippet HTML: snippets/cards.html
```html
<!-- Snippet: Carte Produit Maraîcher / Panier Bio -->
<div class="card card-interactive" style="max-width: 360px;">
  <div style="display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: var(--space-3);">
    <span class="badge badge-bio"><span class="badge-dot"></span> Récolté ce matin</span>
    <span class="font-mono" style="font-weight: 700; color: var(--color-brand-secondary); font-size: var(--text-lg);">1 500 FCFA <span style="font-size: var(--text-xs); color: var(--color-ink-faint); font-weight: 400;">/ kg</span></span>
  </div>
  <h3 class="card-title">Tomate Roma Sélection Sahel</h3>
  <p class="card-subtitle">Variété ancienne paysanne acclimatée · Chair dense, sans traitement chimique</p>
  <div style="display: flex; gap: var(--space-2); margin-top: var(--space-4); align-items: center;">
    <button type="button" class="btn btn-primary" style="flex: 1; justify-content: center;">Ajouter au panier</button>
    <button type="button" class="btn btn-secondary" style="padding: var(--space-3);" title="Fiche variétale">ℹ</button>
  </div>
</div>

<!-- Snippet: Fiche Technique / Module de Cours Pédagogique -->
<div class="card card-sunken" style="max-width: 480px;">
  <div style="display: flex; align-items: center; gap: var(--space-3); margin-bottom: var(--space-3);">
    <span class="badge badge-accent">Module 03</span>
    <span style="font-size: var(--text-xs); color: var(--color-ink-faint); font-weight: 500;">Durée estimée : 45 min</span>
  </div>
  <h3 class="card-title" style="font-size: var(--text-lg);">Fabrication du Compost Thermophile</h3>
  <p style="font-size: var(--text-sm); color: var(--color-ink-soft); line-height: var(--leading-relaxed); margin-bottom: var(--space-4);">
    Règle d'or du ratio Carbone/Azote (C/N = 30), aération passive et montée en température à 65°C pour pasteuriser les adventices.
  </p>
  <a href="#" class="btn btn-secondary" style="display: inline-flex;">Télécharger le protocole imprimable (PDF) &rarr;</a>
</div>
```

### Snippet HTML: snippets/forms.html
```html
<!-- Snippet: Formulaire de commande de semences / Inscription formation -->
<form class="card" style="max-width: 520px;">
  <h3 class="card-title" style="margin-bottom: var(--space-4);">Commande de Semences &amp; Conseil</h3>
  
  <div class="form-group">
    <label for="producer-name" class="form-label">
      Nom du producteur / Maraîcher <span class="required">*</span>
    </label>
    <input type="text" id="producer-name" name="name" class="form-input" placeholder="Ex: Bakary Coulibaly" required />
    <p class="form-hint">Nom du responsable d'exploitation maraîchère.</p>
  </div>

  <div class="form-group">
    <label for="variety-choice" class="form-label">
      Variété sélectionnée <span class="required">*</span>
    </label>
    <select id="variety-choice" name="variety" class="form-select" required>
      <option value="">Sélectionner une variété paysanne...</option>
      <option value="gombo-clemson">Gombo Clemson Spineless (Reproductible)</option>
      <option value="tomate-roma">Tomate Roma Sélection Sahel (Bio)</option>
      <option value="oignon-galmi">Oignon Violet de Galmi (Acclimaté)</option>
      <option value="piment-habanero">Piment Habanero Jaune</option>
    </select>
  </div>

  <div class="form-group">
    <label class="form-label">Plan de culture ou relevé de sol (optionnel)</label>
    <div class="form-file-dropzone">
      <div class="dropzone-title">📄 Déposer un plan de parcelle (PDF, JPG)</div>
      <div class="dropzone-sub">Glisser-déposer ou cliquer pour parcourir (max 10 Mo)</div>
    </div>
  </div>

  <div style="display: flex; gap: var(--space-3); margin-top: var(--space-6); justify-content: flex-end;">
    <button type="button" class="btn btn-secondary">Annuler</button>
    <button type="submit" class="btn btn-primary">Valider la commande</button>
  </div>
</form>
```

### Snippet HTML: snippets/tables.html
```html
<!-- Snippet: Tableau Technique / Dosages / Fiche Variétale -->
<div class="table-container">
  <table class="table-tech">
    <thead>
      <tr>
        <th>Culture / Variété</th>
        <th>Écartement sur rang</th>
        <th>Cycle de récolte</th>
        <th>Rendement moyen cible</th>
        <th>Statut semence</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><strong>Gombo Clemson Spineless</strong></td>
        <td class="num-val">30 cm × 50 cm</td>
        <td class="num-val">55 jours</td>
        <td class="num-val">12 kg / planche</td>
        <td><span class="badge badge-seed"><span class="badge-dot"></span> Reproductible</span></td>
      </tr>
      <tr>
        <td><strong>Oignon Violet de Galmi</strong></td>
        <td class="num-val">10 cm × 15 cm</td>
        <td class="num-val">110 jours</td>
        <td class="num-val">35 kg / planche</td>
        <td><span class="badge badge-bio"><span class="badge-dot"></span> Bio Certifié</span></td>
      </tr>
      <tr>
        <td><strong>Piment Habanero Jaune</strong></td>
        <td class="num-val">40 cm × 60 cm</td>
        <td class="num-val">90 jours</td>
        <td class="num-val">18 kg / planche</td>
        <td><span class="badge badge-stock"><span class="badge-dot"></span> En stock</span></td>
      </tr>
    </tbody>
  </table>
</div>
```

### Composant CSS: components/animations.css
```css
/* ==========================================================================
   ANIMATIONS & EFFETS AU SCROLL (DESIGN SYSTEM UNIFIÉ)
   Comportement calibré sur transition--slide de nako.bio :
   - Montée progressive : translateY(45px) -> translateY(0)
   - Fondu naturel : opacity: 0 -> 1
   - Courbe douce : cubic-bezier(0.2, 0.8, 0.2, 1) sur 0.85s
   - Délais échelonnés par élément : delay-1 à delay-4
   ========================================================================== */

.scroll-reveal {
  opacity: 0;
  transform: translateY(45px);
  transition-property: opacity, transform;
  transition-duration: 0.85s;
  transition-timing-function: cubic-bezier(0.2, 0.8, 0.2, 1);
  will-change: opacity, transform;
}

.scroll-reveal.revealed {
  opacity: 1;
  transform: translateY(0);
}

/* Délais échelonnés naturels */
.delay-1 { transition-delay: 0.12s; }
.delay-2 { transition-delay: 0.24s; }
.delay-3 { transition-delay: 0.36s; }
.delay-4 { transition-delay: 0.48s; }

/* Parallaxe d'arrière-plan ancré en profondeur */
.parallax-bg {
  background-attachment: fixed;
  background-position: center;
  background-repeat: no-repeat;
  background-size: cover;
}

/* Respect des préférences utilisateur (accessibilité) */
@media (prefers-reduced-motion: reduce) {
  .scroll-reveal {
    opacity: 1 !important;
    transform: none !important;
    transition: none !important;
  }
  .parallax-bg {
    background-attachment: scroll !important;
  }
}
```

### Composant CSS: components/badges.css
```css
/* ==========================================================================
   COMPOSANTS STANDARDS : BADGES & STATUTS
   ========================================================================== */

.badge {
  display: inline-flex;
  align-items: center;
  gap: var(--space-1);
  padding: 3px 10px;
  font-family: var(--font-sans);
  font-size: var(--text-xs);
  font-weight: var(--weight-semibold);
  border-radius: var(--radius-full);
  line-height: 1.2;
  border: 1px solid transparent;
  white-space: nowrap;
}

.badge-dot {
  width: 6px;
  height: 6px;
  border-radius: var(--radius-full);
  display: inline-block;
}

/* 1. Badges thématiques de marque */
.badge-brand {
  background: var(--badge-brand-bg, rgba(47, 74, 52, 0.1));
  color: var(--badge-brand-color, var(--color-brand-primary));
  border-color: rgba(47, 74, 52, 0.15);
}
.badge-brand .badge-dot {
  background: var(--color-brand-primary);
}

.badge-accent {
  background: var(--badge-accent-bg, rgba(200, 104, 50, 0.12));
  color: var(--badge-accent-color, var(--color-brand-secondary));
  border-color: rgba(200, 104, 50, 0.2);
}
.badge-accent .badge-dot {
  background: var(--color-brand-secondary);
}

/* 2. Badges Métier Agricole & Maraîcher */
.badge-bio {
  background: var(--color-success-bg);
  color: var(--color-success);
  border-color: rgba(47, 114, 68, 0.25);
}
.badge-bio .badge-dot {
  background: var(--color-success);
}

.badge-seed {
  background: #FDF4E7;
  color: #A54F20;
  border-color: rgba(165, 79, 32, 0.25);
}
.badge-seed .badge-dot {
  background: #C86832;
}

.badge-stock {
  background: #EBF4F9;
  color: var(--color-info);
  border-color: rgba(43, 98, 130, 0.25);
}
.badge-stock .badge-dot {
  background: var(--color-info);
}

/* 3. Badges d'état / feedback fonctionnels */
.badge-success {
  background: var(--color-success-bg);
  color: var(--color-success);
  border-color: rgba(47, 114, 68, 0.2);
}
.badge-success .badge-dot {
  background: var(--color-success);
}

.badge-warning {
  background: var(--color-warning-bg);
  color: var(--color-warning);
  border-color: rgba(201, 122, 30, 0.2);
}
.badge-warning .badge-dot {
  background: var(--color-warning);
}

.badge-danger {
  background: var(--color-danger-bg);
  color: var(--color-danger);
  border-color: rgba(178, 58, 46, 0.2);
}
.badge-danger .badge-dot {
  background: var(--color-danger);
}
```

### Composant CSS: components/buttons.css
```css
/* ==========================================================================
   COMPOSANTS STANDARDS : BOUTONS
   Boutons d'action, navigation et interactions modulaires
   ========================================================================== */

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
  padding: var(--space-3) var(--space-5);
  border-radius: var(--radius-sm);
  font-size: var(--text-sm);
  font-weight: var(--weight-semibold);
  font-family: var(--font-sans);
  text-decoration: none;
  transition: all var(--transition-base, 0.2s ease);
  cursor: pointer;
  border: 1px solid transparent;
  line-height: 1.2;
}

.btn-primary {
  background: var(--color-brand-primary);
  color: var(--color-ink-inverted);
  border-color: var(--color-brand-primary);
}

.btn-primary:hover {
  background: var(--color-brand-primary-dark, var(--color-brand-primary));
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
}

.btn-secondary {
  background: var(--color-surface-card);
  color: var(--color-ink);
  border-color: var(--color-border-strong);
}

.btn-secondary:hover {
  background: var(--color-surface-sunken);
  border-color: var(--color-brand-primary);
  transform: translateY(-1px);
}

.btn-accent {
  background: var(--color-brand-secondary);
  color: #FFFFFF;
  border-color: var(--color-brand-secondary);
}

.btn-accent:hover {
  opacity: 0.92;
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
}

.btn-ghost {
  background: transparent;
  color: var(--color-brand-primary);
  border-color: var(--color-brand-primary);
}

.btn-ghost:hover {
  background: var(--color-brand-primary);
  color: var(--color-ink-inverted);
}

.btn-sm {
  padding: var(--space-1) var(--space-3);
  font-size: var(--text-xs);
}

.btn-lg {
  padding: var(--space-4) var(--space-8);
  font-size: var(--text-base);
}
```

### Composant CSS: components/callouts.css
```css
/* ==========================================================================
   COMPOSANTS STANDARDS : ENCADRÉS (CALLOUTS & ALERTES)
   ========================================================================== */

.callout {
  margin: var(--space-6) 0;
  padding: var(--space-4) var(--space-5);
  background: var(--color-surface-sunken);
  border-left: 4px solid var(--color-brand-primary);
  border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  line-height: var(--leading-relaxed);
  color: var(--color-ink);
}

.callout-title {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  font-weight: var(--weight-bold);
  font-size: var(--text-sm);
  text-transform: uppercase;
  letter-spacing: 0.05em;
  margin-bottom: var(--space-2);
  color: var(--color-brand-primary);
}

.callout-tip {
  border-left-color: var(--color-brand-secondary);
}
.callout-tip .callout-title {
  color: var(--color-brand-secondary);
}

.callout-warning {
  background: var(--color-warning-bg);
  border-left-color: var(--color-warning);
}
.callout-warning .callout-title {
  color: var(--color-warning);
}

.callout-danger {
  background: var(--color-danger-bg);
  border-left-color: var(--color-danger);
}
.callout-danger .callout-title {
  color: var(--color-danger);
}
```

### Composant CSS: components/cards.css
```css
/* ==========================================================================
   COMPOSANTS STANDARDS : CARTES & FICHE DE SYNTHÈSE
   ========================================================================== */

.card {
  background: var(--color-surface-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: var(--space-6);
  box-shadow: var(--shadow-sm);
}

.card-sunken {
  background: var(--color-surface-sunken);
  border: 1px solid rgba(27, 35, 24, 0.08);
}

.card-interactive {
  transition: transform 0.15s ease, box-shadow 0.15s ease, border-color 0.15s ease;
  cursor: pointer;
  text-decoration: none;
  color: inherit;
  display: block;
}

.card-interactive:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
  border-color: var(--color-brand-primary);
}

.card-title {
  font-family: var(--font-serif);
  font-size: var(--text-xl);
  font-weight: var(--weight-bold);
  line-height: var(--leading-tight);
  color: var(--color-ink);
  margin: 0 0 var(--space-2) 0;
}

.card-subtitle {
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  color: var(--color-ink-faint);
  margin: 0 0 var(--space-4) 0;
}
```

### Composant CSS: components/forms.css
```css
/* ==========================================================================
   COMPOSANTS STANDARDS : FORMULAIRES & CHAMPS DE SAISIE
   Champs texte, select, zones de téléversement et labels accessibles
   ========================================================================== */

.form-group {
  margin-bottom: var(--space-5);
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
}

.form-label {
  font-family: var(--font-sans);
  font-size: var(--text-xs);
  font-weight: var(--weight-bold);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--color-ink-soft);
  display: flex;
  align-items: center;
  gap: var(--space-1);
}

.form-label .required {
  color: var(--color-danger);
}

.form-hint {
  font-size: var(--text-xs);
  color: var(--color-ink-faint);
  line-height: var(--leading-snug);
  margin: 0;
}

.form-input,
.form-select,
.form-textarea {
  width: 100%;
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  padding: var(--space-3) var(--space-4);
  background: var(--color-surface-card);
  color: var(--color-ink);
  border: 1px solid var(--color-border-strong);
  border-radius: var(--radius-sm);
  box-shadow: var(--shadow-sm);
  transition: border-color var(--transition-fast), box-shadow var(--transition-fast);
  outline: none;
}

.form-input:focus,
.form-select:focus,
.form-textarea:focus {
  border-color: var(--color-brand-primary);
  box-shadow: 0 0 0 3px rgba(47, 74, 52, 0.15);
}

.form-input::placeholder,
.form-textarea::placeholder {
  color: var(--color-ink-faint);
  opacity: 0.7;
}

.form-file-dropzone {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: var(--space-6);
  border: 2px dashed var(--color-border-strong);
  border-radius: var(--radius-md);
  background: var(--color-surface-sunken);
  cursor: pointer;
  text-align: center;
  gap: var(--space-2);
  transition: all var(--transition-base);
}

.form-file-dropzone:hover {
  background: var(--color-surface-card);
  border-color: var(--color-brand-primary);
}

.dropzone-title {
  font-weight: var(--weight-bold);
  color: var(--color-brand-primary);
  font-size: var(--text-sm);
}

.dropzone-sub {
  font-size: var(--text-xs);
  color: var(--color-ink-faint);
}
```

### Composant CSS: components/steps.css
```css
/* ==========================================================================
   COMPOSANTS STANDARDS : ÉTAPES PÉDAGOGIQUES
   ========================================================================== */

.steps-list {
  display: flex;
  flex-direction: column;
  gap: var(--space-6);
  margin: var(--space-6) 0;
  padding: 0;
  list-style: none;
}

.step-card {
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
  background: var(--color-surface-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: var(--space-6);
  box-shadow: var(--shadow-sm);
  transition: border-color 0.2s ease, box-shadow 0.2s ease;
}

.step-card:hover {
  border-color: var(--color-brand-primary);
  box-shadow: var(--shadow-md);
}

.step-header {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: var(--space-4);
  border-bottom: 1px solid var(--color-border);
  padding-bottom: var(--space-3);
}

.step-meta-group {
  display: flex;
  align-items: baseline;
  gap: var(--space-3);
}

.step-number {
  font-family: var(--font-serif);
  font-size: var(--text-3xl);
  font-weight: var(--weight-bold);
  line-height: 1;
  color: var(--color-brand-primary);
}

.step-title {
  font-family: var(--font-sans);
  font-size: var(--text-lg);
  font-weight: var(--weight-semibold);
  color: var(--color-ink);
  margin: 0;
}

.step-badge {
  display: inline-flex;
  align-items: center;
  gap: var(--space-1);
  padding: var(--space-1) var(--space-3);
  background: var(--color-surface-sunken);
  color: var(--color-ink-soft);
  font-family: var(--font-sans);
  font-size: var(--text-xs);
  font-weight: var(--weight-medium);
  border-radius: var(--radius-full);
}

.step-body {
  font-family: var(--font-sans);
  font-size: var(--text-base);
  line-height: var(--leading-relaxed);
  color: var(--color-ink);
}

.step-body p {
  margin: 0 0 var(--space-3) 0;
}

.step-body p:last-child {
  margin-bottom: 0;
}
```

### Composant CSS: components/tables.css
```css
/* ==========================================================================
   COMPOSANTS STANDARDS : TABLEAUX TECHNIQUES & DOSAGES
   ========================================================================== */

.table-container {
  width: 100%;
  overflow-x: auto;
  margin: var(--space-6) 0;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
}

.table-tech {
  width: 100%;
  border-collapse: collapse;
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  text-align: left;
  background: var(--color-surface-card);
}

.table-tech th {
  background: var(--color-surface-sunken);
  color: var(--color-ink);
  font-weight: var(--weight-semibold);
  padding: var(--space-3) var(--space-4);
  border-bottom: 1px solid var(--color-border-strong);
}

.table-tech td {
  padding: var(--space-3) var(--space-4);
  border-bottom: 1px solid var(--color-border);
  color: var(--color-ink);
}

.table-tech tr:last-child td {
  border-bottom: none;
}

.table-tech tr:hover td {
  background: var(--color-surface-muted);
}

.table-tech .num-val {
  font-family: var(--font-mono);
  font-weight: var(--weight-medium);
}
```

---

## 4. Directives Strictes & Règles IA (AI_CODING_RULES.md)

# Instructions pour Assistants IA & Développeurs (AI_CODING_RULES)

Ce fichier définit les règles strictes de développement front-end et design pour tous les projets du dépôt (Campus, Nako.bio, Biosain, Documents).

---

## 1. RÈGLES TYPOGRAPHIQUES ABSOLUES

- **INTERDICTION STRICTE** d'utiliser `Newsreader`, `Inter`, `Poppins`, `Montserrat`, `Roboto` ou toute autre police non autorisée.
- **SEULES DEUX POLICES SONT AUTORISÉES** :
  - `Playfair Display` (Serif) pour : `h1`, `h2`, `h3`, numéros d'étapes (`01`, `02`), citations et grands chiffres statistiques.
  - `Quicksand` (Sans-serif) pour : tout le reste (`body`, `p`, `h4`, `h5`, boutons, tableaux, badges, formulaires, navigation).
- Utilisez toujours les variables CSS `--font-serif` et `--font-sans`.
- Import Google Fonts obligatoire :
  ```css
  @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,500;0,600;0,700;0,800;1,400;1,600&family=Quicksand:wght@300;400;500;600;700&display=swap');
  ```

---

## 2. GESTION DES COULEURS ET VARIABLES CSS

- **Ne jamais coder en dur** de couleurs hexadécimales dans le CSS des composants.
- Utilisez systématiquement les tokens :
  - Surfaces : `var(--color-surface)`, `var(--color-surface-card)`, `var(--color-surface-sunken)`.
  - Encres : `var(--color-ink)`, `var(--color-ink-soft)`, `var(--color-ink-faint)`.
  - Bordures : `var(--color-border)`.
  - Marques : `var(--color-brand-primary)`, `var(--color-brand-secondary)`, `var(--color-brand-accent)`.
  - Gradients : `var(--gradient-hero)`, `var(--gradient-brand)`, `var(--gradient-accent)`.
- Pour basculer entre les identités de marque, appliquez l'attribut `data-theme="nakobio"` ou `data-theme="biosain"` sur la balise `<html>` ou le conteneur racine.

---

## 3. ANIMATIONS ET TRANSITIONS

- **Ne jamais coder en dur** une durée ou un easing d'animation.
- Utilisez toujours les tokens d'animation :
  - `var(--transition-fast)` : hover sur boutons, badges (100ms)
  - `var(--transition-base)` : la plupart des interactions (200ms) — **valeur par défaut**
  - `var(--transition-slow)` : transitions de section ou de page (350ms)
  - `var(--transition-bounce)` : micro-animations expressives type scale/pop (300ms bounce)
- Glassmorphism : toujours utiliser `backdrop-filter: blur(var(--blur-md))`.
- Respectez `@media (prefers-reduced-motion: reduce)` pour les animations.

---

## 4. STRUCTURE DES COMPOSANTS PÉDAGOGIQUES

- Les étapes de travail doivent utiliser la structure de cartouche `.step-card` :
  ```html
  <article class="step-card">
    <div class="step-header">
      <div class="step-meta-group">
        <span class="step-number">01</span>
        <h3 class="step-title">Titre de l'étape</h3>
      </div>
      <span class="step-badge">20 min</span>
    </div>
    <div class="step-body">
      <p>Instructions claires...</p>
    </div>
  </article>
  ```
- Les avertissements et conseils utilisent `.callout` :
  - Conseil : `.callout.callout-tip`
  - Vigilance : `.callout.callout-warning`
  - Erreur critique : `.callout.callout-danger`

---

## 5. COMPATIBILITÉ PRINT & MOBILE

- Tous les tableaux et fiches techniques doivent rester parfaitement lisibles sur écran mobile (360px de large).
- Toutes les pages de contenu pédagogique doivent supporter l'impression et l'export PDF propre via `print.css` (`@media print`).
